# Server port (defaults to 8080 if not set)
PORT=8080

# Allowed CORS origin (React dev server default)
CORS_ORIGIN=http://localhost:3000

# Reserved for future LLM integration (do not commit real key)
OPENAI_API_KEY=your-openai-api-key
